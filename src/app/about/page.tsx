// app/about/page.tsx
import Container from "../components/container";

export default function About() {
  return (
    <div style={{ padding: '1rem 0' }}>
      <h2>Lab 1 — Project Setup & Routing</h2>
      <p>This page shows that routing works correctly using the App Router.</p>
      <p>Your layout, header, footer, and grid container are now functional.</p>
    </div>
  );
}
